const crypto = require("crypto");

function genPassword(password) {}

function validPassword(password, has, salt) {}

module.exports.validPassword = validPassword;
module.exports.genPassword = genPassword;
